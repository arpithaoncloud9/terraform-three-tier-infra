{
  "name": "aws-3tier-app",
  "version": "1.0.0",
  "description": "AWS 3-Tier Application with CloudWatch Monitoring",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["aws", "terraform", "docker", "cloudwatch"],
  "author": "Maria Arpitha",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "aws-sdk": "^2.1400.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}